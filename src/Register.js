import React, {Component} from 'react';
import {
	BrowserRouter,
	Route,
	Switch,
	NavLink,
	Redirect
} from 'react-router-dom'
import './App.css';

const Register = ({ model }) => {
  return (
    <h2>Register</h2>
  );
};

export default Register;
